spring:
  h2:
    console:
      enabled: true
      path: /h2console
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driverClassName: org.h2.Driver

security:
  jwt:
    token:
      secret-key: secret

springfox:
  documentation:
    swagger-ui:
      base-url: /api

logging:
  level:
    root: debug